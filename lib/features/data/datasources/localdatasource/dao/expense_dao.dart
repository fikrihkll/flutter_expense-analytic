

abstract class ExpenseDaoSQFLite {
  //
  // @insert
  // Future<void> insertLogs(LogTable data);
  //
  // @insert
  // Future<void> createUser(UserTable data);
  //
  // @Query('SELECT*FROM LogTable ORDER BY date DESC LIMIT 10')
  // Future<List<LogTable>> getLatestLogs();
  //
  // @Query('SELECT SUM(nominal) as total FROM LogTable WHERE month = :month AND year = :year')
  // Future<MonthExpenseResult?> getMonthExpense(int month, int year);
  //
  // @Query('SELECT*FROM LogTable WHERE month = :month AND year = :year ORDER BY date DESC LIMIT 10 OFFSET :limit')
  // Future<List<LogTable>> getLogsInMonth(int month, int year, int limit);
  //
  // @Query('DELETE FROM LogTable WHERE id = :id')
  // Future<void> deleteLog(int id);
  //
  // @Query('SELECT category, SUM(nominal) as total FROM LogTable WHERE month = :month AND year = :year GROUP BY category')
  // Future<List<LogDetailResult>> getLogsDetailInMonth(int month, int year);

}